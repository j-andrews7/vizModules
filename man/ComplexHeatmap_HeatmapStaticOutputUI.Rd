% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComplexHeatmap_Heatmap_module_ui.R
\name{ComplexHeatmap_HeatmapStaticOutputUI}
\alias{ComplexHeatmap_HeatmapStaticOutputUI}
\title{Static (non-interactive) heatmap output UI component for the ComplexHeatmap module}
\usage{
ComplexHeatmap_HeatmapStaticOutputUI(
  id,
  resizable = TRUE,
  width = "100\%",
  height = "100\%"
)
}
\arguments{
\item{id}{The ID for the Shiny module. Must match the \code{id} used for
\code{\link[=ComplexHeatmap_HeatmapServer]{ComplexHeatmap_HeatmapServer()}}.}

\item{resizable}{Logical; whether to wrap the plot in a resizable container.
Unlike \code{\link[=ComplexHeatmap_HeatmapOutputUI]{ComplexHeatmap_HeatmapOutputUI()}}, this is honoured, since a
\code{plotOutput} has no resize handle of its own.}

\item{width, height}{Passed to \code{\link[shiny:plotOutput]{shiny::plotOutput()}}. The defaults fill the
containing element, so the heatmap follows its container's size.}
}
\value{
A Shiny UI object for the static heatmap.
}
\description{
Renders the heatmap as a plain \code{\link[shiny:plotOutput]{shiny::plotOutput()}} instead of an
\pkg{InteractiveComplexHeatmap} widget. The same \code{\link[=ComplexHeatmap_HeatmapServer]{ComplexHeatmap_HeatmapServer()}}
call backs both, so switching between them needs no server-side change --
use this function \emph{or} the interactive output functions for a given module
\code{id}, not both.
}
\details{
What is given up is the widget's interactivity: cell hover/click, the
sub-heatmap zoom, and the brush info panel. What is gained is a panel with no
chrome of its own. \pkg{InteractiveComplexHeatmap} draws a grey border around
the heatmap panel, a control tab strip beneath it, and sizes itself in fixed
pixels; none of that can be switched off through an argument, since the
border is set by an id selector in that package's own stylesheet. A
\code{plotOutput} has none of it and fills its container at whatever \code{width} and
\code{height} say, which is what a figure panel wants -- it is how the
\code{ComplexHeatmap} module appears in the Figure Builder (see
\code{\link[=figureBuilderServer]{figureBuilderServer()}}).

Unlike the interactive output, this needs only \pkg{ComplexHeatmap} itself,
not \pkg{InteractiveComplexHeatmap}.
}
\examples{
library(VizModules)
ComplexHeatmap_HeatmapStaticOutputUI("heatmap")
# Fixed size, no resize handle:
ComplexHeatmap_HeatmapStaticOutputUI("heatmap",
    resizable = FALSE, width = "600px", height = "400px"
)
}
\seealso{
\code{\link[=ComplexHeatmap_HeatmapOutputUI]{ComplexHeatmap_HeatmapOutputUI()}} for the interactive widget,
\code{\link[=ComplexHeatmap_HeatmapServer]{ComplexHeatmap_HeatmapServer()}}
}
\author{
Jared Andrews
}
