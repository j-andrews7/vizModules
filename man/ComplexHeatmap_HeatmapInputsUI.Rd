% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComplexHeatmap_Heatmap_module_ui.R
\name{ComplexHeatmap_HeatmapInputsUI}
\alias{ComplexHeatmap_HeatmapInputsUI}
\title{Input UI components for the ComplexHeatmap module}
\usage{
ComplexHeatmap_HeatmapInputsUI(
  id,
  data,
  defaults = NULL,
  title = NULL,
  columns = 2
)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{data}{The data frame used for plot generation, \emph{or} a list with a
\code{matrix} data frame (required) and a \code{column_annotations} data frame
(optional) — see \code{\link[=ComplexHeatmap_HeatmapServer]{ComplexHeatmap_HeatmapServer()}}'s \code{data} parameter for
details. Row-annotation choices come from \code{matrix}'s columns;
column-annotation choices (and the "Column Annotations" tab controls) only
appear when \code{column_annotations} is supplied.}

\item{defaults}{A named list of default values for the inputs. An entry may also be a
\code{\link[shiny:reactive]{shiny::reactive()}} or \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}}; it is resolved with \code{\link[shiny:isolate]{shiny::isolate()}} to
seed the control, and the module then keeps it live (see \code{\link[=setup_reactive_defaults]{setup_reactive_defaults()}}).}

\item{title}{An optional title for the UI grid.}

\item{columns}{Number of columns for the UI grid.}
}
\value{
A Shiny tagList containing the UI elements.
}
\description{
This should be placed in the UI where the inputs should be shown, with an \code{id}
that matches the \code{id} used in the \code{ComplexHeatmap_HeatmapServer()} and
\code{ComplexHeatmap_HeatmapOutputUI()} functions.
}
\details{
Unlike the other plotly-based modules, this module wraps
\code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}} and renders its interactive output via the
\pkg{InteractiveComplexHeatmap} package. The incoming data frame is converted
to a numeric matrix (see the Data / Matrix tab) before being passed to
\code{Heatmap()}.

The inputs are organized into a grid via \code{\link[=organize_inputs]{organize_inputs()}}, with \code{columns}
controlling the number of columns in the grid. Defaults for each input can be
supplied via the \code{defaults} argument (see \code{\link[=get_default]{get_default()}}).
}
\section{Plot parameters and defaults}{

The following \code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}} parameters can be accessed via UI
inputs and/or the \code{defaults} argument:
\itemize{
\item \code{matrix.cols} - Numeric columns forming the matrix (UI: "Matrix Columns",
default: all numeric columns)
\item \code{rowname.col} - Column used as row names (UI: "Row Name Column", default: "")
\item \code{name} - Heatmap / legend title (UI: "Heatmap Name", default: "value")
\item \code{na_col} - Color for \code{NA} cells (UI: "NA Color", default: "grey")
\item \code{scale} - Z-score the matrix by row, column, or not at all (UI: "Scale", default:
"None"). Applied before plotting only — row/column annotation values and the
source-data download always use the unscaled matrix. Scaling happens \emph{after} the Filter
tab's row/column filters, so a Z-score describes only the rows and columns on screen.
\item \code{reverse.palette} - Reverse the palette (UI: "Reverse Palette", default: FALSE)
\item \code{low_color}, \code{mid_color}, \code{high_color} - Colors for the low/mid/high end of the value
scale, i.e. what \code{col} in \code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}} is built from via
\code{\link[circlize:colorRamp2]{circlize::colorRamp2()}} (UI: "Low/Mid/High Color", default: blue/white/red)
\item \code{min_value}, \code{mid_value}, \code{max_value} - Values mapped to \code{low_color}/\code{mid_color}/\code{high_color}
(UI: "Min/Mid/Max Value", default: NA = derived from the matrix: min, \code{mean(range(mat))},
and max respectively)
\item \code{show_heatmap_legend} - Show the heatmap legend (UI: "Show Legend", default: TRUE)
\item \code{border} - Draw heatmap border (UI: "Border", default: FALSE)
\item \code{cluster_rows} - Cluster rows (UI: "Cluster Rows", default: TRUE)
\item \code{cluster_columns} - Cluster columns (UI: "Cluster Columns", default: TRUE)
\item \code{clustering_distance_rows} - Row distance metric (UI: "Row Distance", default: "euclidean")
\item \code{clustering_distance_columns} - Column distance metric (UI: "Column Distance", default: "euclidean")
\item \code{clustering_method_rows} - Row linkage method (UI: "Row Method", default: "complete")
\item \code{clustering_method_columns} - Column linkage method (UI: "Column Method", default: "complete")
\item \code{show_row_dend} - Show row dendrogram (UI: "Show Row Dendrogram", default: TRUE)
\item \code{show_column_dend} - Show column dendrogram (UI: "Show Column Dendrogram", default: TRUE)
\item \code{row_split_by} - Row split method: "None", "K-means", "Hierarchical", or "Annotation" (UI:
"Row Split Method", default: "None"). Only one split mechanism is ever active per axis, which
avoids the error \code{ComplexHeatmap::Heatmap()} raises when both a k-means and a hierarchical
split are requested at once.
\item \code{row_split_n} - Number of row groups, used when \code{row_split_by} is "K-means" or "Hierarchical"
(UI: "Row Groups", default: NA; clamped to the number of matrix rows)
\item \code{row_split_cols} - Columns whose values group the rows, used when \code{row_split_by} is
"Annotation" (UI: "Row Split Columns", default: none). Several columns give nested slices,
one per observed combination. Values come from the same place row annotation tracks read, so
a split and a track on one column always agree.
\item \code{column_split_by} - Column split method: "None", "K-means", "Hierarchical", or "Annotation"
(UI: "Column Split Method", default: "None")
\item \code{column_split_n} - Number of column groups, used when \code{column_split_by} is "K-means" or
"Hierarchical" (UI: "Column Groups", default: NA; clamped to the number of matrix columns)
\item \code{column_split_cols} - Columns of the \code{column_annotations} table whose values group the
heatmap columns, used when \code{column_split_by} is "Annotation" (UI: "Column Split Columns";
only shown when \code{data} supplies a \code{column_annotations} table; default: none)
\item \code{row_gap} - Gap between row slices, mm (UI: "Row Gap (mm)", default: 1)
\item \code{column_gap} - Gap between column slices, mm (UI: "Column Gap (mm)", default: 1)
\item \code{row_title} - Row title (UI: "Row Title", default: "")
\item \code{column_title} - Column title (UI: "Column Title", default: "")
\item \code{show_row_names} - Show row names (UI: "Show Row Names", default: TRUE)
\item \code{show_column_names} - Show column names (UI: "Show Column Names", default: TRUE)
\item \code{row_names_side} - Row names side (UI: "Row Names Side", default: "right")
\item \code{column_names_side} - Column names side (UI: "Column Names Side", default: "bottom")
\item \code{column_names_rot} - Column name rotation (UI: "Column Name Rotation", default: 90)
\item \code{row_names_fontsize} - Row name font size (UI: "Row Name Size", default: 12)
\item \code{column_names_fontsize} - Column name font size (UI: "Column Name Size", default: 12)
\item \code{title_fontsize} - Row/column title font size (UI: "Title Size", default: 13.2)
\item \code{row_annotations} - Row annotation tracks, built as \code{\link[ComplexHeatmap:rowAnnotation]{ComplexHeatmap::rowAnnotation()}} and
passed as \code{left_annotation}/\code{right_annotation} per row (UI: "Annotations" tab, "Row
Annotations" \code{\link[=multiDynamicInput]{multiDynamicInput()}} — each row picks a \code{matrix} column, a side (Left or
Right), and the side and font size of that track's own name label (Bottom or Top, since
ComplexHeatmap places a row annotation's name above or below it), and whether that track
contributes a legend ("Show Legend", default \code{TRUE}); default: none). Each
row's color control appears just below the list once a column is picked: numeric columns
get Low/Mid/High color pickers, everything else gets a \code{\link[=multiColorPicker]{multiColorPicker()}} with one color
per level. Seed those colors from \code{defaults} with a named color vector keyed by the
annotation's \emph{column name} — the row names in a \code{row_annotations}/\code{column_annotations}
default are not stable, since the client reports rows back as \code{row1}, \code{row2}, ...
\item \code{column_key} - Column in \code{column_annotations} matched against the matrix's selected
column names (UI: "Annotations" tab, "Column Key"; only shown when \code{data} supplies a
\code{column_annotations} table)
\item \code{column_annotations} - Column annotation tracks, built as
\code{\link[ComplexHeatmap:columnAnnotation]{ComplexHeatmap::columnAnnotation()}} and passed as \code{top_annotation}/\code{bottom_annotation} per
row (UI: "Annotations" tab, "Column Annotations" \code{\link[=multiDynamicInput]{multiDynamicInput()}} — each row picks a
column, a side (Top or Bottom), the side and font size of that track's own name label
(Right or Left, since ComplexHeatmap places a column annotation's name beside it), and
whether that track contributes a legend ("Show Legend", default \code{TRUE}), with the
same per-row color controls as row annotations; only shown when \code{data} supplies a
\code{column_annotations} table; default: none)
}
}

\section{Plot parameters implementing new functionality}{

The "Filter" tab's two inputs have no \code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}} equivalent — they
narrow the matrix before it is built, so a specific set of genes or samples can be
plotted without wiring up the separate \code{dataFilter} module:
\itemize{
\item \code{row_filter} - Expression keeping only the matching rows (UI: "Row Filter", default: "").
Evaluated against the \code{matrix} data frame, so every one of its columns is in scope —
annotation columns, the row-name column, and the matrix columns themselves.
\item \code{column_filter} - Expression keeping only the matching matrix columns (UI: "Column Filter",
default: ""). Matrix columns are sample names rather than rows of a data frame, so the
expression is evaluated against a frame built with one row per selected matrix column: a
synthetic \code{column} field holding the column name, plus every field of \code{column_annotations}
joined via \code{column_key}. \code{column \%in\% c("Healthy_1", "Healthy_2")} therefore works with no
metadata table at all, while \code{condition == "Disease"} works as soon as one is supplied. If
\code{column_annotations} already has a field named \code{column}, the real one wins and no synthetic
is added.
}

Both are evaluated with \code{\link[=safe_eval_filter]{safe_eval_filter()}}, which permits comparisons, \code{&}/\code{|}/\code{!},
\code{\%in\%}, \code{is.na()}, arithmetic, and the string helpers \code{grepl}, \code{startsWith}, \code{endsWith},
\code{substr}, \code{nchar}, \code{toupper}, \code{tolower}, and \code{trimws}. Anything else — a function call
outside that list, or a symbol that is not a column — is rejected and reported in the UI
rather than evaluated. An expression yielding \code{NA} for a row drops that row.

Filtering runs before everything else: \code{scale}, the annotation tracks, the split methods,
and the source download all describe the filtered matrix.

Both inputs are debounced by 700ms, so the heatmap redraws once you pause rather than on
every keystroke of a half-typed expression.
}

\section{Plot parameters not implemented}{

The following \code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}} parameters are not exposed because
they require R code, objects, or annotations that do not map cleanly to UI
inputs: \code{cell_fun}, \code{layer_fun}, \code{post_fun}, \code{rect_gp}, \code{border_gp},
custom \code{col} mapping functions for the annotation tracks (beyond the Low/Mid/High colors and
\code{multiColorPicker}), \code{row_order} / \code{column_order}, \code{row_labels} / \code{column_labels},
\code{jitter}, and all rasterization parameters (\code{use_raster}, \verb{raster_*}).
}

\examples{
library(VizModules)
ComplexHeatmap_HeatmapInputsUI("heatmap", example_heatmap_matrix)
}
\seealso{
\code{\link[ComplexHeatmap:Heatmap]{ComplexHeatmap::Heatmap()}}, \code{\link[=organize_inputs]{organize_inputs()}},
\code{\link[=ComplexHeatmap_HeatmapOutputUI]{ComplexHeatmap_HeatmapOutputUI()}},
\code{\link[=ComplexHeatmap_HeatmapServer]{ComplexHeatmap_HeatmapServer()}}, \code{\link[=ComplexHeatmap_HeatmapApp]{ComplexHeatmap_HeatmapApp()}}
}
\author{
Jacob Martin, Jared Andrews
}
