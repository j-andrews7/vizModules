% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createModuleApp.R
\name{createModuleApp}
\alias{createModuleApp}
\title{Create an Example Module App from Any Module Trio}
\usage{
createModuleApp(
  inputs_ui_fn,
  output_ui_fn,
  server_fn,
  data_list,
  defaults = NULL,
  hide.inputs = NULL,
  hide.tabs = NULL,
  show.table = TRUE,
  title = "VizModules App",
  primary.table = NULL,
  sidebar.width = 4
)
}
\arguments{
\item{inputs_ui_fn}{A function with signature
\verb{function(id, data, ...)} that returns module input UI elements
(e.g. \code{\link[=plotthis_BarPlotInputsUI]{plotthis_BarPlotInputsUI()}}).}

\item{output_ui_fn}{A function with signature \verb{function(id)} that
returns the module's output UI (e.g. \code{\link[=plotthis_BarPlotOutputUI]{plotthis_BarPlotOutputUI()}}).}

\item{server_fn}{A function with signature
\verb{function(id, data, ...)} that drives the module server logic
(e.g. \code{\link[=plotthis_BarPlotServer]{plotthis_BarPlotServer()}}).}

\item{data_list}{A named list of datasets. Each element is either a data
frame, or a named list of data frames for a module that needs companion
tables alongside the one being filtered — e.g.
\code{list(matrix = , column_annotations = )} for \code{ComplexHeatmap_Heatmap}. Only
the primary table (see \code{primary.table}) is filtered and shown in the Data
Table; the rest are passed through to the module untouched.}

\item{defaults}{A named list of ui ids and their default values that can change the ui default
settings on startup. An entry may also be a \code{\link[shiny:reactive]{shiny::reactive()}} or \code{\link[shiny:reactiveVal]{shiny::reactiveVal()}} to
have the input follow app state; see \code{\link[=setup_reactive_defaults]{setup_reactive_defaults()}}.}

\item{hide.inputs}{A character vector of input IDs to hide. These inputs are still
initialized and their values passed to the plot, but are not shown in the UI.
Passed through to \code{server_fn} when it accepts a \code{hide.inputs} argument.}

\item{hide.tabs}{A character vector of tab names to hide. Inputs in these tabs are
still initialized and their values passed to the plot, but are not shown in the UI.
Passed through to \code{server_fn} when it accepts a \code{hide.tabs} argument.}

\item{show.table}{Logical. When \code{TRUE} (default), a filterable DT table is
shown below the plot and its row selection drives the data passed to the
plot module. When \code{FALSE}, the table and filter controls are hidden and
the full (unfiltered) dataset is passed directly to the plot module.}

\item{title}{A character string used as the page title
(default: \code{"VizModules App"}).}

\item{primary.table}{For a \code{data_list} entry that is a list of tables, the
name of the one to filter and show in the Data Table. Defaults to the first
data frame in the entry. Ignored for entries that are a plain data frame.}

\item{sidebar.width}{Bootstrap column width (1-11) for the controls sidebar;
the plot area takes the rest. Raise the plot's share for a module whose
output needs room, e.g. \code{sidebar.width = 3} for the heatmap.}
}
\value{
A \code{\link[shiny:shinyApp]{shiny::shinyApp()}} object.
}
\description{
Factory function that generates a standard Shiny application for any
VizModules module.
The resulting app features a \strong{Data Import} section for uploading
data files, a \strong{Data Table} for viewing and editing the active
dataset, and a \strong{Plot} area for configuring and displaying an interactive
plot.
}
\details{
Uploaded files (Excel, CSV, TSV, or tab-delimited text) are added to the
available datasets and can be selected for plotting.
If an uploaded file shares a name with an existing dataset, the existing
one is overwritten with a warning.

Every module-specific \verb{*App()} convenience function (e.g.
\code{\link[=plotthis_BarPlotApp]{plotthis_BarPlotApp()}}, \code{\link[=linePlotApp]{linePlotApp()}}) is a thin wrapper around
\code{createModuleApp()}.
You can also call it directly for quick prototyping or to create apps
for custom wrapper modules.
}
\examples{
library(VizModules)

# Quick-launch a bar plot app with custom data:
app <- createModuleApp(
    inputs_ui_fn = plotthis_BarPlotInputsUI,
    output_ui_fn = plotthis_BarPlotOutputUI,
    server_fn    = plotthis_BarPlotServer,
    data_list    = list("iris" = iris),
    title        = "My Bar Plot",
    defaults     = NULL
)
if (interactive()) runApp(app)

# Works with any module trio, including custom wrapper modules:
app2 <- createModuleApp(
    inputs_ui_fn = dittoViz_scatterPlotInputsUI,
    output_ui_fn = dittoViz_scatterPlotOutputUI,
    server_fn    = dittoViz_scatterPlotServer,
    data_list    = list("iris" = iris),
    title        = "Scatter",
    defaults    = NULL
)
if (interactive()) runApp(app2)
}
\author{
Jared Andrews
}
