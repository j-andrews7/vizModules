% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComplexHeatmap_Heatmap_module_ui.R
\name{ComplexHeatmap_HeatmapOutputUI}
\alias{ComplexHeatmap_HeatmapOutputUI}
\title{Output UI components for the ComplexHeatmap module}
\usage{
ComplexHeatmap_HeatmapOutputUI(id, resizable = TRUE, fit.width = TRUE, ...)
}
\arguments{
\item{id}{The ID for the Shiny module.}

\item{resizable}{Logical; accepted for signature parity with the other module
output functions but ignored, since the InteractiveComplexHeatmap widget
manages its own sizing.}

\item{fit.width}{Logical; when \code{TRUE} (the default) the widget's panels are
scaled once on load so the whole thing fits the width of its container,
instead of sitting at the fixed pixel widths baked in by
\pkg{InteractiveComplexHeatmap}. \code{width1}/\code{width2} (and their defaults)
then act as the relative widths that get scaled, rather than absolute
sizes. Heights are left alone, and the widget's own resize handle and size
tab still override the fitted width afterwards. Pass \code{FALSE} for fixed
pixel widths.}

\item{...}{Additional arguments passed to
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}}, e.g.
\code{layout}, \code{compact}, \code{width1}/\code{height1}, \code{title1}/\code{title2}/\code{title3}.}
}
\value{
A Shiny UI object for the interactive heatmap.
}
\description{
This should be placed in the UI where the heatmap should be shown. Unlike the
plotly modules, the interactive output is provided by
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}}, which supplies
its own resize and export controls.
}
\details{
This renders the original heatmap, the selected sub-heatmap, and the
click/brush info panel together as one widget, arranged per \code{layout} (see
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}} for the
available layout strings, e.g. \code{"(1-2)|3"}, \code{"1|(2-3)"}, \code{"1-2-3"}).

Pass \code{compact = TRUE} for a smaller footprint (see the
\href{https://jokergoo.github.io/InteractiveComplexHeatmap/articles/shiny_dev.html#compact-mode}{"Compact
mode" article section}): the sub-heatmap panel is dropped entirely and the
click/brush info floats near the cursor instead of occupying its own static
area — equivalent to \verb{response = c(action, "brush-output"), output_ui_float = TRUE}, per \code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}}'s
own docs. \code{layout} has nothing left to arrange in compact mode, since only
one static panel remains. No server-side change is needed to turn compact
mode on or off.

A floating info panel is moved onto \verb{<body>} by \pkg{InteractiveComplexHeatmap}
and parked off-screen when idle. It is parked to the \emph{left} here rather than
to the right as the package does, since a box parked past the right edge
extends the host page's scrollable width by 10,000px.

To place the three components independently anywhere in a custom UI
(separate tabs, cards, columns, etc.), use \code{\link[=ComplexHeatmap_HeatmapMainOutputUI]{ComplexHeatmap_HeatmapMainOutputUI()}},
\code{\link[=ComplexHeatmap_HeatmapSubOutputUI]{ComplexHeatmap_HeatmapSubOutputUI()}}, and \code{\link[=ComplexHeatmap_HeatmapInfoOutputUI]{ComplexHeatmap_HeatmapInfoOutputUI()}}
instead of this function. Use one approach or the other, not both, for the
same module \code{id}. Compact mode is specific to this combined widget: the
separated pieces (\code{originalHeatmapOutput()}, \code{subHeatmapOutput()},
\code{HeatmapInfoOutput()}, which back the three functions above) don't accept a
\code{compact} argument at all.
}
\examples{
library(VizModules)
# Default combined widget:
ComplexHeatmap_HeatmapOutputUI("heatmap")
# Same widget, main heatmap on its own row above sub-heatmap + info:
ComplexHeatmap_HeatmapOutputUI("heatmap", layout = "1|(2-3)")
# Compact: no sub-heatmap panel, click/brush info floats near the cursor
ComplexHeatmap_HeatmapOutputUI("heatmap", compact = TRUE)
# Fixed pixel widths, ignoring the container:
ComplexHeatmap_HeatmapOutputUI("heatmap", fit.width = FALSE)
}
\seealso{
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}},
\code{\link[=ComplexHeatmap_HeatmapMainOutputUI]{ComplexHeatmap_HeatmapMainOutputUI()}}, \code{\link[=ComplexHeatmap_HeatmapSubOutputUI]{ComplexHeatmap_HeatmapSubOutputUI()}},
\code{\link[=ComplexHeatmap_HeatmapInfoOutputUI]{ComplexHeatmap_HeatmapInfoOutputUI()}}
}
\author{
Jacob Martin, Jared Andrews
}
