% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComplexHeatmap_Heatmap_module_ui.R
\name{heatmap_fit_width}
\alias{heatmap_fit_width}
\title{Fit a hand-built InteractiveComplexHeatmap widget to its container's width}
\usage{
heatmap_fit_width(
  ui,
  heatmap_id,
  panels = c("heatmap", "sub_heatmap"),
  output = TRUE
)
}
\arguments{
\item{ui}{The UI returned by
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}} (or the
\code{originalHeatmapOutput()}/\code{subHeatmapOutput()} pair).}

\item{heatmap_id}{The \code{heatmap_id} passed to that output function.}

\item{panels}{Character vector of panels to scale. Defaults to both; pass
just \code{"heatmap"} for a \code{compact = TRUE} widget, which has no sub-heatmap.}

\item{output}{Logical; also scale the click/brush info panel. Defaults to
\code{TRUE}, matching what \code{\link[=ComplexHeatmap_HeatmapOutputUI]{ComplexHeatmap_HeatmapOutputUI()}} does for the same
combined widget; pass \code{FALSE} when the app lays that panel out itself.}
}
\value{
\code{ui}, with the fitting script and its dependency attached.
}
\description{
The heatmap module's output functions already do this (see their \code{fit.width}
argument). This is the same behaviour for an app that calls
\code{\link[InteractiveComplexHeatmap:InteractiveComplexHeatmapOutput]{InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput()}} itself rather
than going through the module — a heatmap driven directly by
\code{\link[InteractiveComplexHeatmap:makeInteractiveComplexHeatmap]{InteractiveComplexHeatmap::makeInteractiveComplexHeatmap()}}, say.
}
\details{
\code{InteractiveComplexHeatmapOutput()} bakes its \code{width1}/\code{width2} into the page
as fixed pixels, so the widget over- or under-fills whatever room the app
actually gives it until the resize handle is dragged. Wrapping the output in
this rescales the panels to their container once the page is laid out; the
widget's own resize controls still win afterwards.

A widget on a tab that is not the active one has no width to measure on load.
It is fitted when its container is first laid out instead, so a heatmap the
user has not opened yet is still correct the moment they do.
}
\examples{
if (interactive() && requireNamespace("InteractiveComplexHeatmap", quietly = TRUE)) {
    heatmap_fit_width(
        InteractiveComplexHeatmap::InteractiveComplexHeatmapOutput(
            heatmap_id = "my_ht", width1 = 1480, height1 = 500
        ),
        heatmap_id = "my_ht"
    )
}

}
\seealso{
\code{\link[=ComplexHeatmap_HeatmapOutputUI]{ComplexHeatmap_HeatmapOutputUI()}}, whose \code{fit.width} argument is
the module-side equivalent.
}
\author{
Jared Andrews
}
